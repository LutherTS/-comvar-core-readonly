// import { composedVariablesExclusives } from "../../comment-variables/index.js";

/* general bases */

/** The absolute prefix for every single Comment Variables placeholder. */
export const $COMMENT = "$COMMENT";

/* process environments */

/** The shared `env` between `fresh-import-a.js` and `fresh-import-b.js` for the `freshImport` utility. */
export const MODULE_TO_LOAD = "MODULE_TO_LOAD";

// /* zod discriminated unions */

// // VariationsDataPreSchema

// export const UNION = composedVariablesExclusives.variables.union.value;
// export const BOTH = composedVariablesExclusives.variables.both.value;
// export const NEITHER = composedVariablesExclusives.variables.neither.value;
